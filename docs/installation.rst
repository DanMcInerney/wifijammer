============
Installation
============

At the command line::

    $ easy_install wifijammer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wifijammer
    $ pip install wifijammer
